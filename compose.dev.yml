name: limen-dev
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: limen_dev
      POSTGRES_USER: limen
      POSTGRES_PASSWORD: limen
    ports:
      - "5433:5432"
